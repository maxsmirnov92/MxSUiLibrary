<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/loading"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="@dimen/loading_container_margin">

    <ProgressBar
        android:id="@+id/pbLoading"
        style="?android:attr/progressBarStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:indeterminate="true"
        android:indeterminateTint="@color/colorProgressBarPrimary"
        android:indeterminateTintMode="src_atop" />

    <TextView
        android:id="@+id/tvLoadingMessage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/loading_message_padding"
        android:layout_marginLeft="@dimen/loading_message_padding"
        android:padding="@dimen/loading_message_margin"
        android:text="@string/loading"
        android:textColor="@color/textColorLoadingMessage"
        android:textSize="@dimen/loading_message_text_size"
        app:fontFamily="sans-serif" />

</LinearLayout>